const assert = require("assert");

const Tickers = require("../../../../src/network/web/coinbase/ticker");

describe("network/web/coinbase || Ticker Test", () => {
  // it("should fetch prices", () => {
  //   Tickers.mainnet.update();

  //   setInterval(() => {
  //     Tickers.mainnet._tokens.forEach(token => {
  //       console.log(
  //         Tickers.mainnet.getPrice(token.address)
  //       );
  //     });
  //   }, 500);
  // }).timeout(5000);
});
